#include<stdio.h>
int main(){
    int i=1;
    printf("%d \n",i++);//this mean frist i will be printed then increment value 
                    // by one and the store in complier 
    printf("%d",i);//now this will give the increment value                 
}